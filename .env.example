NODE_ENV=development
PORT=3000
DB_HOST=localhost
DB_USER=your_db_user
DB_PORT=5432
DB_DATABASE=your_database
DB_PASSWORD=your_db_password
LOG_LEVEL=info
# DB_TYPE: postgres (default/prod), sqlite (file db), or sqlite:memory (in-mem for tests). Ignored in prod (only postgres allowed).
DB_TYPE=postgres
